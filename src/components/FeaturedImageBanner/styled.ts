import styled from "styled-components"

export const Container = styled.div`
  position: fixed;
  bottom: 0;
  left: 0;
  box-sizing: border-box;
  padding: 1em 0.5em;
  pointer-events: none;
  p {
    margin: 0;
    color: gray;
  }
`